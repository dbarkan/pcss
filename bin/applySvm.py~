import unittest
import configobj
import pcssTools
import pcssPeptide
import os
import pcssFeatures
import pcssFeatureHandlers
import pcssErrors
import logging
import sys
import pcssTests
import pcssSvm
import pcssIO

logging.basicConfig(stream=sys.stdout)
logging.root.setLevel(logging.DEBUG)

#parse peptides -- get parser from config, have peptide input file
#run peptides through pcss -- maybe time for runner to have execute and subclassing
#runner should handle global exceptions and others
#then do apply svm
